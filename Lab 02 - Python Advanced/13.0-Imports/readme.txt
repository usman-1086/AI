To Install all libraries from requirements.txt file, write the following command in the terminal:

pip install -r requirements.txt


Packages in Python

Create a folder called package
Write your code module inside package folder